import 'package:flutter/material.dart';

class LastWeekHistory extends StatelessWidget {
  const LastWeekHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
